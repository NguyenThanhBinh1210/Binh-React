import { useSelector } from 'react-redux'

export function useAuthenticated() {
	// Truy cập vào reduxx trên trình duyệt, khi mình đăng nhập vào nó sẽ có cái _id , name, email j đó
	return useSelector(state => )
}
